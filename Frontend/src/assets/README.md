# Asset Plan

This project now keeps assets grouped by type for maintainability:

- `assets/images/campus/` – homepage and marketing campus photos
- `assets/images/branding/` – logos, marks, and branded graphics
- `assets/icons/` – reusable icon assets

Current migrated files still in `assets/` root can be moved incrementally during migration.
