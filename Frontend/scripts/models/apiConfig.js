/**
 * @fileoverview Central configuration for API communication.
 * Defines the root endpoints and shared configuration constants used by 
 * all data models in the application.
 * @module scripts/models/apiConfig
 */
/**
 * The base URL for all API requests throughout the application.
 * Currently points to the local development server.
 * @type {string}
 */
export const API_BASE_URL = "/api/v1";
